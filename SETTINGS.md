# 말씀 곁에 v3.0 현재 설정

이 문서는 현재 구현된 설정의 스냅샷이다. 실제 값은 각 소스 파일이 기준이다.

## 화면

- 기존 종이색 `#faf9f5`, 짙은 녹색 `#244d43` 유지. 버전 v3.0 하단 중앙.
- 성경 PDF·TXT·TSV 다운로드는 우측 상단, 공유는 링크 복사.
- ‘오늘의 질문 TOP’은 환영 문구 바로 위. 1~9위 번호를 붙인 3열×3줄 박스.
- 순위는 1분마다 재조회. 한국시간 `YYYY.MM.DD HH:mm update` 표시.
- LIVE 동그라미는 오렌지 `#df842f`, 3초 주기 밝기 변화.
- 순위 등장 효과는 10초마다 반복. 박스당 0.786초, 순차 지연 157ms.
- 동작 줄이기 설정에서는 애니메이션 생략.
- 초기 질문 100개 중 3개 표시. ‘다른 질문’은 직전 3개를 제외하고 무작위 선택.
- 답변 이후에는 Qwen이 만든 맥락별 추천 질문 3개 표시. 누르면 입력만 하고 자동 전송하지 않음.
- 생성 중 입력창 하단: ‘성경말씀을 살피고 있어요’와 점 애니메이션.
- 안내문은 기존 12px에서 2pt 축소.

## 응답과 모델

- Ollama `qwen3:14b`, `think:false`, temperature 0.35, num_ctx 16384, num_predict 1800.
- 첫 답변은 말씀 → 해석 → 응답. 해석·응답 각각 보통 3~5문장.
- 후속 답변은 보통 4~8문장, 2~3문단. 기존 대비 약 30% 더 설명하도록 지시.
- 후속 말투: 따뜻함 80%, 가벼운 유머 10%, 순한 시니컬함 10%를 방향으로 삼음. 정확한 비율을 보장하는 수치는 아님.
- 사용자를 비꼬지 않으며 위기·상실·학대·기도문에서는 농담 생략.
- 필요한 경우 제공된 성경 문맥의 장절을 문장 안에 1~2곳 표시하도록 지시.
- 욕설·성희롱 등 부적절한 요청에는 자제 안내, 이해 불가 입력에는 재설명 요청.
- 진지한 피해 경험이나 성 관련 상담을 단어만으로 차단하지 않음.
- 대화 초기화는 브라우저 대화 문맥과 추천 질문을 초기 상태로 복귀시킴.

## 이용 한도 및 저장

- 전체 이용자 합산 정상 응답 일일 100회. 한국시간 완료 날짜 기준, 자정에 새 한도.
- 동시 생성 1개. 스트리밍 모델 생성 제한 240초, 브라우저 제한 255초.
- 운영 DB는 `~/.local/share/bible-counsel/quota.sqlite3`. Git에는 포함하지 않음.
- 최근 100회 성공 응답의 질문·답변 원문과 완료 시각을 보관. 101번째부터 가장 오래된 쌍 삭제.
- 원문은 즉시 삭제되지 않으며 사용자가 직접 입력한 개인정보가 포함될 가능성이 있음.
- 대화 초기화 버튼은 서버 저장 기록을 삭제하지 않음.
- 키워드는 별도로 저장하지 않고 최근 질문에서 규칙 기반으로 재계산. 모델 의미 분석은 아님.
- 가족관계·불안심리·소통문제 등 허용된 주제명만 공개. 최근 20회 성공 응답의 질문으로 순위를 계산. 첫 기록부터 집계하며 20회 충족을 기다리지 않음.
- 삭제 시 secure_delete 사용 및 WAL 체크포인트 시도. 백업·디스크 전체의 영구 소거를 보장하지 않음.

### 화면 안내와 실제 동작의 차이

사용자 요청으로 화면에는 아래 문구를 표시한다. 이는 ‘추출 직후 삭제’를 뜻하는 것으로 읽힐 수 있으나,
실제 구현은 위의 최근 100회 원문 보관이다. 화면 문구만 바뀌었으며 저장 정책은 바뀌지 않았다.

> 대화는 개인을 식별하지 않으며, 원문은 TOP키워드 추출 후 삭제합니다.
> 또한, AI는 전문 상담을 대신하지 않음을 말씀드립니다.

## 실행 및 배포

- `python3 server/app.py`, 로컬 127.0.0.1:8765. Ollama는 127.0.0.1:11434.
- 모델·포트·DB 경로는 COUNSEL_MODEL, COUNSEL_PORT, COUNSEL_QUOTA_DB로 지정 가능.
- 공개 API 주소는 counsel-config.js가 기준. 임시 터널 재시작 시 주소 갱신 필요.
- master 푸시 후 GitHub Actions가 share.html을 생성하고 공개 정적 파일만 Pages에 배포.
- 실행 프로세스·모델 파일·운영 DB·인증 정보는 Git 푸시로 저장되거나 복구되지 않음.
- 한국시간 매일 06시 재시작은 이전에 자동화 제안만 제공됨. 활성화된 일정으로 확인된 상태는 아님.

## 스트리밍 적용

- 브라우저는 `/api/chat`에 `Accept: application/x-ndjson`을 보낸다.
- `partial` 이벤트는 미완성 답변, `done`은 저장·차감 완료된 최종 답변, `error`는 생성 실패다.
- JSON 필드가 완성되기 전에도 응답 문자열을 표시한다. disposition 확인 전에는 본문을 보내지 않는다.
- 중단·실패 시 미완성 응답을 제거하고 입력 내용을 보존한다. 완료 전에는 대화 기록에 추가하지 않는다.
- 원문 보관·일일 횟수는 최종 성공 때만 커밋한다. 연결 종료 시점에 따라 이미 완료된 응답은 차감될 수 있다.
- 구형 클라이언트는 기존 JSON 응답 유지. 최근 최대 9개 대화 쌍, 약 18,000자까지 프런트에서 전달한다.
- 16,384 문맥은 로컬 Qwen 시험 후 적용. 실제 전달 범위는 메시지 길이와 토큰화에 따라 달라진다.

## macOS 자동 복구 운영

- LaunchAgent `kr.bible-counsel.supervisor`가 로그인 시 실행되고 종료 시 launchd가 재실행한다.
- Documents 접근 제한을 피하도록 실제 운영 체크아웃은 `~/Library/Application Support/BibleCounsel/runtime`에 둔다.
- supervisor가 상담 서버·임시 터널 종료 코드와 실행 시간을 기록하고 5초 후 재시작한다.
- 로그: `~/Library/Application Support/BibleCounsel/lifecycle.log`, 파일당 2MB, 이전 5개 보관.
- 상담 원문과 임의의 오류 본문은 로그에 쓰지 않는다. 오류 발생 여부·종료 코드만 남긴다.
- 터널 주소 변경 시 counsel-config.js만 커밋하여 master에 푸시한다. 인증·충돌·네트워크 오류는 로그에 기록한다.
- 설정 파일: `~/Library/LaunchAgents/kr.bible-counsel.supervisor.plist`.
- 이후 서버 코드 변경 시 운영 체크아웃에도 반영해야 한다. Documents의 소스 수정만으로 실행 중 코드가 바뀌지 않는다.
- 로그인 전·로그아웃·절전·전원 종료 동안 가용성을 보장하지 않는다. Ollama 앱은 별도로 실행되어야 한다.

## v2.1 대화 원칙 (이전 분량·유머 설정 대체)

- 첫 답변의 말씀 → 해석 → 응답 구조는 유지하고 고정 문장 수와 30% 분량 증가 지시는 제거.
- 후속 대화에서 공감 → 조언 → 질문 공식을 반복하지 않음. 필요에 따라 짧게 또는 충분히 응답.
- 유머·시니컬함 10% 비율을 제거하고 상황에 맞을 때만 가벼운 재치 허용.
- 사용자의 구체적인 표현을 이어받고, 감정과 사연을 추측해 단정하지 않음.
- 자책을 덜어주는 현실적인 관점, 강요하지 않는 작은 선택, 필요한 경우에만 성경 근거 연결.
- 과장된 감동·회복 보장·인간 경험 사칭 없이 따뜻한 해요체 유지.
- 토큰 한도 1800, 문맥 16384, 스트리밍 및 안전 안내는 유지.


## 2026-09-09 — v2.1 안전 대응·후속 지침 보완

- `server/safety.py`: 명확한 위험 신호에 서버 고정 안내. 자해·폭력 위기는 모델·생성 잠금·일일 한도에 관계없이 응답하고 일일 생성 횟수/TOP 통계에 기록하지 않는다.
- 약물 임의 변경·AI 독점 의존·정체성·대화 종료도 별도 안내. 추천 질문 없음.
- Qwen의 안전 disposition은 서버 안내로 대체하며 생성된 위험 문장을 미리 스트리밍하지 않는다.
- 후속 문체는 40대 남성의 차분하고 친근한 말투를 참고. 실제 사람이나 목사로 가장하지 않음.
- 최신 요청과 정정을 우선. 후속 단순 대화·듣기에는 검색 성경 본문을 전달하지 않음. 필요한 성경 요청에만 본문 연결. 첫 답변 형식 유지.
- 실험용 예시 100개는 운영에 넣지 않음. 모델·생성 한도·문맥 크기는 기존대로 유지.
- 한계와 참고 자료: `server/SAFETY.md`. 표현 규칙과 모델 분류가 놓치는 위험까지 완벽히 방지한다고 보장하지 않음.


## 2026-09-10 — 시작 질문 교체

예상 질문 목록 100개를 시작 질문으로 교체했다. 한 번에 무작위 3개를 세로 한 열로 표시하며 질문 전체를 줄바꿈한다. 다른 질문 버튼은 직전 3개를 제외하고 다시 고른다. 후속 추천 질문 배치는 유지한다. 긴 시작 질문도 표시하도록 초기 질문 길이 검사를 분리했다.
