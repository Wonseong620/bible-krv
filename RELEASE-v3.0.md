# 말씀 곁에 v3.0

2026-09-10

현재 공개 상담 화면과 운영 설정을 v3.0으로 묶었습니다. 기존 종이색 배경과 짙은 녹색 디자인을 유지합니다.

## 포함 내용

- 초기 예시 질문 100개와 세로 3행 선택 화면, 대화에 연결되는 후속 질문.
- 오늘의 질문 TOP, 상담 응답 표시, 홈 화면 추가 및 링크 복사 기능.
- 첫 답변과 후속 대화를 구분하는 상담 지침 및 안전 대응.
- 서버 점검 시 안내 표시와 일반 상담 요청 차단. 긴급 상황의 고정 안전 안내는 유지합니다.
- 화면 하단 버전 및 API 버전을 3.0으로 통일하고 CSS·JavaScript 캐시 주소를 갱신했습니다.

## LoRA 실험 결과와 운영 모델

공개 상담은 기존 Ollama `qwen3:14b`를 사용합니다. LoRA 학습본은 운영에 적용하지 않았습니다.

수정·검수한 가상 예시 58개로 Qwen3-14B 계열의 MLX 4비트 모델에 LoRA 학습을 실시했습니다. 58회 업데이트로 추가 파라미터 1,048,576개를 학습했습니다. 기존 기반 가중치는 고정했습니다.

별도 질문 12개의 비교 결과는 LoRA 우세 3개, 기존 모델 우세 4개, 동률 1개, 양쪽 모두 수정 필요 4개입니다. LoRA 출력에서 JSON 오류 3개와 안전 대응 보완 사례가 확인되어 배포를 보류했습니다. 이 평가는 AI 편집 검토이며 독립 전문가 검수를 뜻하지 않습니다. 실제 이용자의 대화는 학습에 사용하지 않았습니다.

실험 결과 수치는 `release/v3.0/lora-evaluation.json`에 보관합니다. 학습 모델 파일과 로컬 운영 데이터는 이 웹 릴리즈에 포함하지 않습니다.
